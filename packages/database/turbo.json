{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["db:generate"],
      "outputs": ["dist"]
    },
    "db:generate": {
      "outputs": ["src/generated"]
    }
  },
  "extends": ["//"]
}